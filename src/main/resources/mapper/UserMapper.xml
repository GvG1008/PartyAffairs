<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zqu.pa.dao.UserDao">
	<select id="getUser" resultType="User">
	   select * from user_login where state = 1 limit 1
	</select>
	<select id="getUserLogin" resultType="User" parameterType="String">
       select * from user_login where state = 1 and user_id = #{userId}
	</select>
	<select id="getRolePermission" resultType="String" parameterType="INT">
		select permission_name
		from role_permission a
		LEFT JOIN permission_menu b ON a.permission = b.permission_id
		where role_id = #{roleId}
	</select>
</mapper>